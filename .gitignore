.DS_Store
PROJECT/PDDA-ACTIVITY.jsonl
# Cached GitHub issue-state for `pdda.sh issue-doc-sync` (regenerated by pdda-gh-refresh.sh; never tracked)
.pdda-gh-state.tsv
# pdda-sync.sh HQ-side runtime state: state stamps, manifest snapshots, backups, run log, lock.
# (The target registry is NOT here — it is machine-local under ~/.config/pdda/registry.tsv.)
/temp/
# Install registry is machine-local (~/.config/pdda/); never track a stray in-repo .config
.config/
# aider/consult local session artifacts (chat + input history, tag cache) — never track
.aider*
.xyz/
# relay/consult run transcripts (machine-local audit output) — never track
relay-system/

# Aider Studio (auto-added)
REPO_MAP.md
# Sentinel Phase 2a spike transient outputs (model responses, doc copies, results) — never track
sentinel/spike/work/
# XYZ marathon runtime state (tick coordinator + per-phase work dirs) — never track
.tick/
/phases/
ESCALATION.md
