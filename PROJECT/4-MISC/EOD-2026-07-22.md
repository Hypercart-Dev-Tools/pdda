---
title: EOD 2026-07-22 — PDDA-EOD hardening shipped; Sentinel Phase 0 GO; inbox triaged
status: Complete (4-MISC)
created: 2026-07-22
updated: 2026-07-22
owner: noel
doc_type: eod
window: 2026-07-22 00:00 PDT → 21:30 PDT
related:
  - PROJECT/3-COMPLETED/GH-6-PDDA-EOD.md
  - PROJECT/2-WORKING/GH-50-SENTINEL-GEMMA-CONSOLIDATION.md
  - PROJECT/1-INBOX/GH-51-PDDA-DOC-INDEX-SPIKE.md
---

# EOD — 2026-07-22

First EOD summary in this repo. Written by the `/pdda-eod` wrap it documents.

## Status

| What was just completed | What's next |
|---|---|
| Four PRs merged to `main` (#49, #52, #53, #54). `/pdda-eod` hardened into a verified delivery-closure flow and installed device-wide. Inbox reconciled: #50 reopened, one stale plan archived, #55 filed. | Sentinel Phase 1 (adopt cactus agents into the 3-Eyes registry) under #50. Fix the CWD-relative path defect shared by #47/#55. Decide whether GH-14 Phase 2 is still worth doing given today's live evidence. |

## What shipped

All four PRs touched today are **merged into `origin/main`**, which ended the day at `f56f87e`.

| PR | Branch | What landed |
|---|---|---|
| [#54](https://github.com/Hypercart-Dev-Tools/pdda/pull/54) | `feat/pdda-eod-delivery-closure` | GH-6: `/pdda-eod` hardened into end-to-end delivery closure (`547e654`) |
| [#53](https://github.com/Hypercart-Dev-Tools/pdda/pull/53) | `feat/gh-51-doc-index-spike` | GH-51 device-wide doc-index spike — plan doc |
| [#52](https://github.com/Hypercart-Dev-Tools/pdda/pull/52) | `feat/gh-50-phase0-bakeoff` | Sentinel Phase 0 bake-off, **GO** verdict |
| [#49](https://github.com/Hypercart-Dev-Tools/pdda/pull/49) | `gh48-governance-hang-fix` | GH-48 governance-check stall fix |

`SKILLS/PDDA-EOD/` was also symlinked to `~/.claude/skills/pdda-eod`, making the skill available in
every repo on this device.

## Reconciliation performed

- **#50 reopened.** `issue-doc-sync` flagged that #50 was CLOSED while its doc sat in `2-WORKING`. The
  deterministic recommendation was to archive the doc; the evidence said the opposite. #50 was closed
  `COMPLETED` when Phase 0's GO verdict landed, but Phase 0 is a *blocking gate* on the consolidation,
  not the consolidation — Phases 1-5 are unstarted. Issue reopened, doc kept in `2-WORKING`, status
  corrected (its Status table also had completed/next inverted).
- **`MARATHON-PLAN-2026-07-07.md` archived to `4-MISC`.** Never fired, 15 days stale, both lanes
  overtaken: lane p1 (myriad reader) shipped and #11 is CLOSED; lane p2 (agents-builder) moved to its
  own capture under open #42.
- **GH-10 and GH-14 kept in `2-WORKING`** despite stale-date warnings — both were triaged as live, not
  abandoned. See below.
- **#55 filed** and cross-linked to #47.

## Stale-doc triage (evidence, not dates)

The stale check flags on age alone. Two of its three hits are live work:

- **GH-10-SENTINEL** (16d) — Phase 2b of 8 complete, Phase 3 (replay harness) next; #10 open; running
  in observe mode. Today's Phase 0 bake-off is downstream of it. **Keep active.** Note the overlap
  with #50: GH-50 Phase 4 supersedes `MULTI-REPO-CACTUS-SENTINEL-DEPLOYMENT.md`, and the GH-10/GH-50
  boundary should be settled before Phase 2 work starts.
- **GH-14-GOVERNANCE-FD-EXHAUSTION** (14d) — Phase 1 shipped (`9e27e54`, fd fix at `pdda.sh:695`);
  Phase 2 (BUG-001b, surface check-level runtime failures) still open. **Keep active.**
- **MARATHON-PLAN-2026-07-07** (15d) — genuinely dead. **Archived.**

## Open findings carried forward

1. **`pdda.sh run` reports `issue-doc-sync` from a stale cache, confidently, in both directions.**
   The aggregate reads `.pdda-gh-state.tsv`, which only `gh-refresh` updates. Both failure directions
   were observed within one hour today:
   - *False clean:* the aggregate reported `errors=0 warns=0` while the same check, run standalone
     with `gh` reachable, found the real #50 drift.
   - *False alarm:* after #50 was reopened, the aggregate still reported the #50 warning from cache
     while the live check was clean.

   In neither case did it degrade to the documented "state unavailable … sync NOT evaluated" path — it
   asserted a definite answer from stale data. Live evidence for GH-14 Phase 2 / #43: a check that did
   not actually evaluate must not be summarized as passing. **Run `pdda.sh gh-refresh` before trusting
   `run`'s issue-doc-sync line.** (Done at the end of this wrap; the aggregate then agreed with the
   live check.)
2. **#47 / #55 — CWD-relative path defect** in both the `pdda` and `PDDA-EOD` skills. Should share one
   root-resolution helper.
3. **`~/.claude/skills/myriad` is a broken symlink** — it points at `SKILLS/myriad` in this repo, which
   does not exist (`SKILLS/` holds only `PDDA-EOD/`, `PDDA-hook/`, `RELEASE-PLAN/`). Device hygiene,
   not a repo defect; the myriad reader shipped under #11 elsewhere. Needs repointing or removing.
4. **Pre-existing check warnings**, unchanged today: `ROADMAP.md` is 271 lines (>200), and `RELEASES.md`
   carries `TBD` target dates for 1.3.0 and 1.4.0.

## Delivery state

This wrap's own edits are on `chore/eod-2026-07-22-wrap`, cut from `origin/main` @ `f56f87e`, delivered
by PR. The repo ends the day with no unpushed commits and no linked worktrees.
